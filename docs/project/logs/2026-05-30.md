# 2026-05-30 Worklog

## 오늘 목표
- Windows/macOS 양쪽에서 프로젝트 실행과 검증 명령이 안정적으로 동작하도록 정리
- Windows PowerShell 한글 인코딩 깨짐을 줄이는 저장소 설정과 가이드 추가

## 수행 작업
- 로컬 `.CMD` shim에 직접 의존하지 않는 `scripts/task-runner.mjs` 추가
- `pnpm dev`, `pnpm build`, `pnpm check` 등 주요 스크립트를 태스크 러너 경유로 정리
- Windows Turbopack 프로세스 접근 거부 이슈를 피하도록 Next.js dev/build를 Webpack 모드로 고정
- `.editorconfig`, `.gitattributes`, `scripts/utf8.ps1` 추가로 UTF-8/LF 정책과 PowerShell 세션 보정 제공
- Windows/macOS 개발 환경 가이드 문서와 README Quick Start 업데이트
- Biome 검사 범위에서 `.next`, coverage, report 등 산출물 제외

## 검증 결과
- 태스크 러너 단위 테스트 통과
- `scripts/task-runner.mjs check` 통과
- `scripts/task-runner.mjs build` Webpack 모드 통과
- PowerShell에서 `scripts/utf8.ps1` 적용 후 README 한글 출력 정상 확인

## 배포/운영 이슈
- 현재 로컬 환경의 PATH에는 `pnpm`/`npm`이 없고 WindowsApps `node.exe` 접근이 거부되어 Codex 번들 Node로 검증함
- Recharts가 정적 빌드 중 ResponsiveContainer 크기 경고를 출력하지만 빌드는 성공함

## 다음 액션
- Recharts SSR 빌드 경고 제거를 위해 차트 클라이언트 마운트 처리 또는 컴포넌트 분리 검토
- 실제 Windows/macOS 개발 머신에서 `pnpm install`, `pnpm check`, `pnpm dev` 재확인
